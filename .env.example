NODE_ENV=development
PORT=4000
API_PREFIX=/api/v1
CORS_ORIGIN=http://localhost:3000

JWT_SECRET=change-me-andresromana
JWT_EXPIRES_IN=8h

DATABASE_URL=
DB_HOST=localhost
DB_PORT=5432
DB_NAME=tickets_management
DB_USER=postgres
DB_PASSWORD=postgres
DATABASE_SCHEMA=tms
DB_POOL_MAX=10

LOG_LEVEL=info
# En desarrollo los logs salen en una línea (método, ruta, status, ms). En producción, JSON compacto sin headers ni JWT.
